# 🎯 دليل تصحيح الأخطاء من خلال Console

## 📱 فتح Developer Tools

### على Chrome / Firefox / Edge
```
Windows/Linux: Ctrl + Shift + I  أو  F12
Mac:          Cmd + Option + I   أو  Cmd + Option + U (للـ Firefox)
```

### ثم انتقل إلى Tab `Console` 📋

---

## 🔍 الدوال التشخيصية المتاحة

### 1️⃣ فحص صحة الرابط

```javascript
// في الـ Console
await StreamingManager.validateStreamUrl('https://example.com/stream.m3u8');
```

**النتيجة المتوقعة:**
```
🔍 فحص صحة الرابط: https://example.com/stream.m3u8
✅ الرابط صالح وقابل للوصول
→ true
```

**إذا كان هناك مشكلة:**
```
🔍 فحص صحة الرابط: https://example.com/missing.m3u8
⚠️ تحذير في فحص الرابط: HTTP 404: الرابط غير موجود أو غير متاح
→ true (نحاول على أي حال)
```

---

### 2️⃣ تشخيص مفصل للرابط

```javascript
// في الـ Console
const diag = await StreamingManager.diagnoseStreamUrl('https://example.com/stream.m3u8');
```

**النتيجة المتوقعة:**
```
🔧 تشخيص مفصل للرابط: https://example.com/stream.m3u8

┌─────────────────────────────────────────┐
│ (index) │ Values                          │
├─────────────────────────────────────────┤
│ url     │ 'https://example.com/stream.m3u8' │
│ isValid │ true                            │
│ canAccess │ true                          │
│ isM3U8  │ true                            │
│ httpStatus │ 200                          │
│ suggestions │ []                          │
└─────────────────────────────────────────┘
```

**إذا كان هناك خطأ:**
```
┌─────────────────────────────────────────┐
│ httpStatus │ 404                          │
│ suggestions │ [                           │
│             "❌ الرابط غير موجود (404)",   │
│             "💡 تحقق من صحة الرابط..."    │
│           ]                              │
└─────────────────────────────────────────┘
```

---

### 3️⃣ استخراج رابط البث من محتوى

```javascript
// في الـ Console
const item = StreamingManager.contentList[0];
const url = StreamingManager.extractPlayUrl(item);
console.log('رابط البث المستخرج:', url);
```

**النتيجة:**
```
رابط البث المستخرج: https://example.com/stream.m3u8
```

**إذا لم يوجد رابط:**
```
رابط البث المستخرج: null
⚠️ المحتوى لا يحتوي على رابط بث
```

---

### 4️⃣ عرض كل المحتوى المحمل

```javascript
// في الـ Console
console.table(StreamingManager.contentList);
```

**النتيجة:** جدول بـ 11 عمود (في حالتك 11 مسلسل)

```
┌──────┬─────────────┬──────────┬────────────┬──────────────┐
│ (index) │ name        │ type     │ group      │ play_url     │
├──────┼─────────────┼──────────┼────────────┼──────────────┤
│ 0    │ MBC 1       │ tv       │ عربي       │ https://...  │
│ 1    │ AlArabiya   │ tv       │ أخبار      │ https://...  │
│ 2    │ Series XYZ  │ series   │ دراما      │ https://...  │
└──────┴─────────────┴──────────┴────────────┴──────────────┘
```

---

### 5️⃣ بحث عن محتوى

```javascript
// في الـ Console
const results = StreamingManager.searchContent('MBC');
console.table(results);
```

**النتيجة:** تعرض جميع المحتويات التي تحتوي على كلمة "MBC"

---

### 6️⃣ الحصول على محتوى حسب النوع

```javascript
// في الـ Console
const series = StreamingManager.getContentByType('series');
console.log(`عدد المسلسلات: ${series.length}`);
console.table(series);
```

**النتيجة:**
```
عدد المسلسلات: 3
┌──────┬────────────────┬────────┐
│ name │ type           │ group  │
├──────┼────────────────┼────────┤
│ M1   │ series         │ drama  │
│ M2   │ series         │ drama  │
│ M3   │ series         │ comedy │
└──────┴────────────────┴────────┘
```

---

## 🎬 تتبع تشغيل الفيديو

### مراقبة الأحداث HLS

```javascript
// في الـ Console
// بعد تشغيل الفيديو، ستشاهد رسائل تلقائية مثل:

🎬 اكتشف بث HLS/M3U8 - يتم التحضير...
HLS manifest loaded successfully
✅ تم تحميل manifest HLS
📊 عدد مستويات الجودة: 3
🎬 اختيار المستوى: 1280x720
▶️ البث يشتغل...
```

---

### الأخطاء الشائعة وحلولها

#### ❌ خطأ: NotSupportedError

```
❌ خطأ في التشغيل: NotSupportedError: No supported source was found
```

**السبب:** Browser لا يدعم هذا نوع الملف

**الحل:**
```javascript
// تحقق من دعم Hls.js
console.log('Hls.isSupported():', Hls.isSupported());

// النتيجة المتوقعة:
// Chrome/Firefox: true
// Safari: false (لكن Safari يدعم HLS native)
```

#### ❌ خطأ: 404 Not Found

```
❌ levelLoadError: 404 Not Found - index_1.m3u8
```

**السبب:** جودة محددة غير موجودة

**الحل - اضبط سلوك الأخطاء:**
```javascript
// سيتم التعامل معها تلقائياً:
// - تجاهل الجودة المفقودة
// - استخدام جودة متاحة أخرى
// - استمرار البث

// شاهد المستويات المتاحة:
console.table(StreamingManager.currentHls?.levels);
```

#### ❌ خطأ: CORS

```
❌ CORS error: Access to XMLHttpRequest blocked
```

**الحل:**
```javascript
// تم إضافة xhrSetup في config:
xhrSetup: (xhr, url) => {
    xhr.withCredentials = true;
}

// هذا يعني الرابط يعمل تلقائياً
```

---

## 📊 الأوامر المفيدة للتصحيح

### عرض حالة البث الحالية

```javascript
// الفيديو الجاري تشغيله
const video = document.getElementById('video-player');
console.log({
    'الحالة': video.paused ? 'متوقف' : 'يشتغل',
    'الموضع (ثانية)': video.currentTime.toFixed(2),
    'المدة': video.duration.toFixed(2),
    'الجودة': video.videoWidth + 'x' + video.videoHeight,
    'المخزن المؤقت (%)': (video.buffered.length > 0 ? 
        (video.buffered.end(0) / video.duration * 100).toFixed(1) : 0) + '%'
});
```

**النتيجة:**
```
{
  الحالة: "يشتغل"
  الموضع (ثانية): "45.23"
  المدة: "2525.45"
  الجودة: "1280x720"
  المخزن المؤقت (%): "87.5%"
}
```

---

### التحقق من معلومات Hls.js

```javascript
// الحصول على معلومات HLS
const hls = StreamingManager.currentHls;
if (hls) {
    console.log({
        'المستويات المتاحة': hls.levels.length,
        'المستوى الحالي': hls.currentLevel,
        'الـ Bitrate': hls.levels[hls.currentLevel]?.bitrate,
        'حالة التحميل': hls.autoLevelCapping,
        'الحدث الأخير': hls.lastError
    });
}
```

---

## 🔧 أوامر تطويرية مفيدة

### إعادة تحميل البث

```javascript
// إذا توقف البث فجأة
StreamingManager.currentHls?.startLoad();
```

### تغيير جودة البث يدويا

```javascript
// اختر جودة معينة (0 = أعلى جودة)
StreamingManager.currentHls?.nextLevel = 0;
```

### إيقاف مراقبة الصحة

```javascript
// إذا كنت تريد إيقاف المراقبة
clearInterval(StreamingManager.healthMonitorId);
```

---

## 📈 نصائح للأداء الأفضل

### 1. تجنب فتح Console كثيراً 📊

```javascript
// بدلاً من:
console.log('البث يعمل');
console.log('الوقت:', new Date());

// استخدم group لتنظيم الرسائل:
console.group('حالة البث');
console.log('✅ البث يعمل');
console.log('⏱️ الوقت:', new Date());
console.groupEnd();
```

### 2. استخدم الفلترة في Console 🔍

```
بحث في Console عن:
✅ (للنجاحات)
❌ (للأخطاء)
⚠️ (للتحذيرات)
```

### 3. احفظ الرسائل عند الإغلاق 💾

```
في Console:
Settings (الترس) → Preserve log
هكذا الرسائل لن تختفي عند التنقل
```

---

## 🚀 مثال كامل للتصحيح

```javascript
// 1. تحقق من المحتوى المحمل
console.log('📊 عدد المحتوى:', StreamingManager.contentList.length);

// 2. اختر محتوى
const item = StreamingManager.contentList[0];
console.log('🎬 المحتوى المختار:', item.name);

// 3. استخرج الرابط
const url = StreamingManager.extractPlayUrl(item);
console.log('🔗 الرابط:', url);

// 4. فحص الرابط
const valid = await StreamingManager.validateStreamUrl(url);
console.log('✅ صحة الرابط:', valid);

// 5. تشخيص مفصل
const diag = await StreamingManager.diagnoseStreamUrl(url);
console.table(diag);

// 6. إذا كانت الرسائل تحذيرات، حاول الحل
if (!diag.canAccess) {
    console.log('💡 يجب حل مشكلة الرابط قبل التشغيل');
}
```

---

## 📝 ملاحظات هامة

1. **الدوال async:** استخدم `await` مع الدوال التي تحتوي على الكلمة `async`

```javascript
// ❌ خطأ
const result = StreamingManager.validateStreamUrl(url);

// ✅ صحيح
const result = await StreamingManager.validateStreamUrl(url);
```

2. **الإتاحة العامة:** جميع الدوال متاحة من Console لأنها في فئة عامة

3. **الأداء:** فحص الرابط قد يستغرق 2-5 ثوان

---

**آخر تحديث:** 23 ديسمبر 2025  
**الإصدار:** v1.0
