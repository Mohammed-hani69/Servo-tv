# SERVO-TV: نظام إدارة الاشتراكات والأجهزة

نظام متكامل لإدارة اشتراكات IPTV والأجهزة، يدعم ثلاث مستويات من المستخدمين: المسؤولين والموزعين والمستخدمين.

## 🚀 المميزات

- ✅ نظام معاملات متعدد المستويات (Admin, Reseller, User)
- ✅ إدارة أكواد التفعيل والاشتراكات
- ✅ تتبع الأجهزة وحالتها
- ✅ نظام النقاط والأرصدة
- ✅ سجلات التدقيق الكاملة (Audit Log)
- ✅ دعم العربية كاملاً
- ✅ API RESTful حديثة
- ✅ قاعدة بيانات SQLAlchemy ORM

## 📋 المتطلبات

- Python 3.8+
- Flask 2.3.3+
- SQLAlchemy 2.0+
- Flask-SQLAlchemy 3.0+

## 🔧 التثبيت السريع

### 1. استنساخ المشروع
```bash
git clone <repository-url>
cd SERVO-TV
```

### 2. إنشاء بيئة افتراضية
```bash
python -m venv venv
# على Windows
.\venv\Scripts\Activate.ps1
# على macOS/Linux
source venv/bin/activate
```

### 3. تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 4. تهيئة قاعدة البيانات
```bash
python wsgi.py
```

### 5. تشغيل التطبيق
```bash
python app.py
```

التطبيق سيكون متاحاً على: http://localhost:5000

## 📚 الهيكل

```
SERVO-TV/
├── app.py              # ملف التطبيق الرئيسي
├── config.py           # الإعدادات
├── models.py           # نماذج قاعدة البيانات
├── init_db.py          # تهيئة البيانات التجريبية
├── wsgi.py             # نقطة WSGI
├── requirements.txt    # المكتبات
├── .env                # متغيرات البيئة
├── routes/             # المسارات
│   ├── admin.py
│   ├── reseller.py
│   └── users.py
├── templates/          # قوالب HTML
└── static/             # ملفات CSS و JS
```

## 🔐 حسابات الاختبار
  "name": "أحمد",
  "email": "ahmed@example.com",
  "age": 25
}
```

### الحصول على مستخدم محدد
```
GET /api/users/<id>
```

### تحديث بيانات مستخدم
```
PUT /api/users/<id>
Content-Type: application/json

{
  "name": "أحمد محمد",
  "email": "ahmed.new@example.com",
  "age": 26
}
```

### حذف مستخدم
```
DELETE /api/users/<id>
```

## 📁 هيكل المشروع

```
SERVO-TV/
├── app.py                 # التطبيق الرئيسي
├── requirements.txt       # المتطلبات
├── README.md             # هذا الملف
├── templates/
│   └── index.html        # الصفحة الرئيسية
└── database.db           # قاعدة البيانات (يتم إنشاؤها تلقائياً)
```

## 🗄️ نموذج البيانات

### جدول Users

| العمود | النوع | الوصف |
|-------|-------|-------|
| id | Integer | المعرف الفريد (المفتاح الأساسي) |
| name | String | اسم المستخدم |
| email | String | البريد الإلكتروني (فريد) |
| age | Integer | عمر المستخدم |
| created_at | DateTime | تاريخ الإنشاء |

## 🎨 الميزات الإضافية

- تصميم جميل باستخدام Gradient
- معالجة أخطاء شاملة
- التحقق من صحة البيانات المدخلة
- واجهة مستخدم سهلة وآمنة

## 📝 الملاحظات

- قاعدة البيانات تُنشأ تلقائياً عند تشغيل التطبيق
- البريد الإلكتروني يجب أن يكون فريداً
- العمر اختياري عند إضافة مستخدم جديد

## 🤝 المساهمة

لا تتردد في تحسين المشروع وإضافة ميزات جديدة!

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.
