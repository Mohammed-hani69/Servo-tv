
// Set this to FALSE to connect to the local Node.js backend
// Set this to TRUE to use the mock data (browser-only mode)
export const USE_MOCK_DATA = true;

export const API_BASE_URL = 'http://localhost:5000/api';
